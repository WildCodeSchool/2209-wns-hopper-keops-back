export function mergeObject(object1: object, object2: object): object {
  const newObject = {};
  // for (const key of Object.keys(object1)) {
  //   newObject[key] = object1[key];
  // }
  // for (const key of Object.keys(object2)) {
  //   newObject[key] = object2[key];
  // }
  return newObject;
}
